import{r as a,o as n,c as s,a as t,F as o,b as c}from"./app.7b3f0e3b.js";import{_ as d}from"./plugin-vue_export-helper.21dcd24c.js";const i={},l=c(`<h1 id="unisign-docs" tabindex="-1"><a class="header-anchor" href="#unisign-docs" aria-hidden="true">#</a> UniSign Docs</h1><h2 id="commands" tabindex="-1"><a class="header-anchor" href="#commands" aria-hidden="true">#</a> Commands</h2><p><strong>Install</strong></p><div class="language-bash ext-sh line-numbers-mode"><pre class="language-bash"><code><span class="token function">npm</span> <span class="token function">install</span>
</code></pre><div class="line-numbers"><span class="line-number">1</span><br></div></div><p><strong>Local Development</strong></p><div class="language-bash ext-sh line-numbers-mode"><pre class="language-bash"><code><span class="token function">npm</span> run dev
</code></pre><div class="line-numbers"><span class="line-number">1</span><br></div></div><p><strong>Build</strong></p><div class="language-bash ext-sh line-numbers-mode"><pre class="language-bash"><code><span class="token function">npm</span> run build
</code></pre><div class="line-numbers"><span class="line-number">1</span><br></div></div><h2 id="add-document" tabindex="-1"><a class="header-anchor" href="#add-document" aria-hidden="true">#</a> Add document</h2><p>If you want to add a document, please do the following things:</p><ol><li>Add a new <code>.md</code> file in this repo.</li><li>Add the corresponding other language version of the doc, if necessary.</li><li>Update sidebar manu in <a href="./.vuepress/locales/en.ts">./.vuepress/locales/en.ts</a></li></ol><h2 id="github-actions" tabindex="-1"><a class="header-anchor" href="#github-actions" aria-hidden="true">#</a> GitHub actions</h2><p>We are using GitHub actions to build &amp; publish docs. Once the <code>main</code> branch is pushed to GitHub, <code>deploy</code> action will do the following things:</p><ol><li>Fetch the latest version of code</li><li>Install dependencies</li><li>Build docs</li><li>Deploy docs under <code>.vuepress/dist</code> to GitHub pages, with target branch <code>gh-pages</code></li></ol><h2 id="redirect" tabindex="-1"><a class="header-anchor" href="#redirect" aria-hidden="true">#</a> Redirect</h2><p>If we moved a doc to a more proper place and want to keep the old route working, then we need <code>&lt;Redirect&gt;</code>. <code>&lt;Redirect&gt;</code> is a component registered locally, you can simply put it in the old doc, and users visiting the old doc will be redirected to the new doc.</p><div class="language-jsx ext-jsx line-numbers-mode"><pre class="language-jsx"><code><span class="token comment">// /path-to/legacy-doc.md</span>
<span class="token tag"><span class="token tag"><span class="token punctuation">&lt;</span><span class="token class-name">Redirect</span></span> <span class="token attr-name">to</span><span class="token attr-value"><span class="token punctuation attr-equals">=</span><span class="token punctuation">&quot;</span>/path-to/new-doc.md<span class="token punctuation">&quot;</span></span><span class="token punctuation">/&gt;</span></span>
</code></pre><div class="line-numbers"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><p>In the example above, when users enter <code>/path-to/legacy-doc</code>, they will be redirected to <code>/path-to/new-doc</code> automatically. In this way, we can keep the legacy path work.</p>`,18);function r(p,u){const e=a("Redirect");return n(),s(o,null,[l,t(e,{to:"/zh/guide"})],64)}var g=d(i,[["render",r]]);export{g as default};
